# Auto-generated by Spring Boot Universal Migrator
name: OpenRewrite Spring Boot Upgrade

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upgrade:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Run OpenRewrite
        run: |
          echo "Running OpenRewrite in api-gateway"
          cd "api-gateway"
          mvn -B org.openrewrite.maven:rewrite-maven-plugin:run \
            -Drewrite.activeRecipes=org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_4,org.openrewrite.java.migrate.Java8toJava21 \
            -Drewrite.exportDatatables=true \
            --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "Running OpenRewrite in common-lib"
          cd "common-lib"
          mvn -B org.openrewrite.maven:rewrite-maven-plugin:run \
            -Drewrite.activeRecipes=org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_4,org.openrewrite.java.migrate.Java8toJava21 \
            -Drewrite.exportDatatables=true \
            --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "Running OpenRewrite in email-service"
          cd "email-service"
          mvn -B org.openrewrite.maven:rewrite-maven-plugin:run \
            -Drewrite.activeRecipes=org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_4,org.openrewrite.java.migrate.Java8toJava21 \
            -Drewrite.exportDatatables=true \
            --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "Running OpenRewrite in identity-service"
          cd "identity-service"
          mvn -B org.openrewrite.maven:rewrite-maven-plugin:run \
            -Drewrite.activeRecipes=org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_4,org.openrewrite.java.migrate.Java8toJava21 \
            -Drewrite.exportDatatables=true \
            --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "Running OpenRewrite in order-service"
          cd "order-service"
          mvn -B org.openrewrite.maven:rewrite-maven-plugin:run \
            -Drewrite.activeRecipes=org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_4,org.openrewrite.java.migrate.Java8toJava21 \
            -Drewrite.exportDatatables=true \
            --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "Running OpenRewrite in payment-service"
          cd "payment-service"
          mvn -B org.openrewrite.maven:rewrite-maven-plugin:run \
            -Drewrite.activeRecipes=org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_4,org.openrewrite.java.migrate.Java8toJava21 \
            -Drewrite.exportDatatables=true \
            --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "Running OpenRewrite in product-service"
          cd "product-service"
          mvn -B org.openrewrite.maven:rewrite-maven-plugin:run \
            -Drewrite.activeRecipes=org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_4,org.openrewrite.java.migrate.Java8toJava21 \
            -Drewrite.exportDatatables=true \
            --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "Running OpenRewrite in service-registry"
          cd "service-registry"
          mvn -B org.openrewrite.maven:rewrite-maven-plugin:run \
            -Drewrite.activeRecipes=org.openrewrite.java.spring.boot3.UpgradeSpringBoot_3_4,org.openrewrite.java.migrate.Java8toJava21 \
            -Drewrite.exportDatatables=true \
            --no-transfer-progress || true
          cd $GITHUB_WORKSPACE

      - name: Commit and push changes
        run: |
          git config --global user.name "Universal Migrator Bot"
          git config --global user.email "migrator@noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "OpenRewrite: Upgrade to Spring Boot 3.4 + Java 21"
          git push
